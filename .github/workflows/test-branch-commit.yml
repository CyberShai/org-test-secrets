name: Branch creation

on: pull_request

env:
  GH_TOKEN: ${{ github.TOKEN }}

jobs:
  create-branch:
    runs-on: ubuntu-latest
    outputs:
      new_branch: ${{ steps.new_branch.outputs.branch }}
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v2
        with:
          token: ${{ env.GH_TOKEN }}

      - name: Create branch name
        id: new_branch
        run: |
          NEW_BRANCH="new-branch-test"
          echo Creating new branch $NEW_BRANCH
          echo "::set-output name=branch::$NEW_BRANCH"

      - name: Save branch
        run: |
          echo "New branch: ${{ steps.new_branch.outputs.branch }}"
          git checkout -b ${{ steps.new_branch.outputs.branch }}
          git push origin ${{ steps.new_branch.outputs.branch }}

  make-commit:
    runs-on: ubuntu-latest
    needs: [create-branch]
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v2
        with:
          token: ${{ env.GH_TOKEN }}

      - name: Get branch
        run: |
          echo git branch -a

      - name: Create fake commit
        run: |
          echo Creating new commit

# Crear rama y hacer commit vacío

# Obtener rama nuevamente y listarla (buscar listar ramas de origen)
